services:
  backend:
    image: ghcr.io/shalin-shah-2002/devflow-backend:latest
    container_name: devflow-backend
    restart: always
    env_file:
      - .env
    ports:
      - "${PORT:-3001}:3001"
